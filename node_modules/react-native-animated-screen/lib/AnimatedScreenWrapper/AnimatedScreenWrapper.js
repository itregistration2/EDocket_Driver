import React, { useState } from 'react';
import { initialWindowMetrics } from 'react-native-safe-area-context';
import { Animated, } from 'react-native';
import AnimatedScreenContext from '../AnimatedScreenContext';
const AnimatedScreenWrapper = ({ children, onScroll, headerMaxHeight = 200, headerMinHeight = 70, avoidSafeArea = false, disableParallaxEffect, }) => {
    const [scrollY] = useState(new Animated.Value(0));
    const { top: safeTop } = initialWindowMetrics?.insets || { top: 0 };
    return (React.createElement(AnimatedScreenContext.Provider, { value: {
            headerMaxHeight: headerMaxHeight + (avoidSafeArea ? 0 : safeTop),
            headerMinHeight,
            onScroll,
            scrollY,
            disableParallaxEffect,
        } }, children));
};
export default React.memo(AnimatedScreenWrapper);
